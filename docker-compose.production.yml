version: '3'

volumes:
  pg_data:
  static:
  media:

services:
  db_kittygram:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgres/data
  backend:
    image: kashichkin/kittygram_backend
    env_file: .env
    volumes:
      - static:/static/
      - media:/app/media/
  frontend:
    env_file: .env
    image: kashichkin/kittygram_frontend
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static
  gateway:
    image: kashichkin/kittygram_gateway
    env_file: .env
    ports:
      - 9090:80
    volumes:
      - static:/static
      - media:/app/media/